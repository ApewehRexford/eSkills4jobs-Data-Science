# Ask the user to enter their first name and surname in lower case. Change the case to title case
#  and join them together. Display the finished result.


user_fname = input("Enter your first name in lower case: ").capitalize()
user_sname = input("Enter your surname in lower case: ").capitalize()

print(user_fname, user_sname)
